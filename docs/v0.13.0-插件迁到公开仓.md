# v0.13.0 — 插件源码迁到公开仓 socrates-pen

> 日期：2026-08-21。插件 semver 仍是 0.12.13（只搬家，不改行为）。

## 做什么

把 `obsidian/` 里的插件源码一次拷到平行公开仓 `/Users/tangyiq/dev/socrates-pen`（`xesws/socrates-pen`）的**根目录**。之后插件的唯一活源码在公开仓。本仓继续只养 sidecar。

## 不做

- 不把 `pen/`、`web/`、手册放进公开仓
- 不打 GitHub Release、不提交社区目录
- 本轮不删本仓 `obsidian/`（只在 README 声明已迁出）

## 公开仓必改

- `esbuild`：production 打到仓根；dev 必须设 `VAULT_PLUGIN_DIR`，禁止写死本机测试库路径
- `check-limits`：sidecar 源码在 `../Socrates-agent` 或 `SOCRATES_AGENT` 时才对表；公开 CI 上跳过
- 保留已有市场 README，只追加 Development

## 工作区

`socrates-pen.code-workspace` 的 plugin 文件夹改为 `../socrates-pen`。
