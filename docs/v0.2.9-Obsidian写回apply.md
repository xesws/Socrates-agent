# v0.2.9 — Obsidian 确认后 apply 进原文，并跳到插入处

> 芯片「把刚才的解答写进手册原文」必须真写盘。用户靠笔记里的 `🔍 实例 N` 看位置，不靠 git。日期：2026-08-20。

## 做什么

- `propose` JSON 带 `insert_after_line`、`beat`。
- 插件：人话位置 + 确认写入 / 取消；apply 前 save 打开的叶子；写入后跳到该行。
- 撤销刚才写入 → sidecar rollback。不提供 git commit 勾选。

## 不做什么

不重算 stale-line（propose 与 apply 之间改笔记仍可能偏）。不改 `insert.py` 算法。
