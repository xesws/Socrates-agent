# v0.2.0 — Obsidian 插件 O-1（本仓 `obsidian/` + 测试库工作区）

> 依据四路 Kimi K3 交叉审查与 `docs/v0.1.5-Obsidian插件对照.md`。日期：2026-08-18。

## 做什么

- 本仓新增 `obsidian/`（esbuild + TS），产物打进本地测试库。
- 编辑器工作区：`socrates-pen.code-workspace`。
- 测试库：`/Users/tangyiq/dev/socrates-pen-vault/`（不进 git）。
- sidecar：CORS 加 Obsidian origin；`PEN_HOME` 可覆盖 `.pen/` 落点。
- 插件：CM6 框选 → SSE 对话 → 原生 Markdown 气泡 → 状态/用量 → 新开会话 → propose 预览。
- **不 apply 真笔记。**

## 工作区怎么开

1. 编辑器打开 `/Users/tangyiq/dev/Socrates-agent/socrates-pen.code-workspace`
2. Obsidian 打开 `/Users/tangyiq/dev/socrates-pen-vault`
3. `python -m pen --host 127.0.0.1 --port 8765`
4. `cd obsidian && npm run dev`
5. 社区插件里启用 Socrates Pen 与 Hot Reload（若已装）

## 不做

新 remote、submodule、worktree、社区送审、插件内存 key、apply。
