# v0.2.7 — 助手气泡里的表格要看得出格子

> 对照 v0.2.6 真机：表格被 MarkdownRenderer 画出来了，但没有边框，窄侧栏里像一堆错位纯文本。日期：2026-08-20。
> 性质：样式。不改 sidecar。

只给 `.socrates-pen .sp-turn.is-assistant .sp-body` 里的 `table/th/td` 加 collapse、边框、内边距。
