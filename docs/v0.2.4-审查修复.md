# v0.2.4 — 按 Kimi 审查修完 v0.2.3 的问题

> 对照 `c418d85` 之后那次整包审查。日期：2026-08-20。
> 性质：修审查清单，不改产品方向。设置页仍是唯一用户入口。

## 修什么

| 原级别 | 修法 |
|---|---|
| 高：供应商错误变 500 / 轨迹记成功 | `stream_chat` / `propose_fold_md` 接住 OpenAI 错误；SSE 出 `error`；`ok=False`；propose 变 400 |
| 中：`extra_roots` 无回归 | import 带 `vault_root` 后断言 `stream_chat` 收到该根 |
| 低：模型能读 `.obsidian/**/data.json` | `assert_readable` 拒绝路径里带 `.obsidian` |
| 低：空钥匙 + 换了主机仍用 env key | 请求 `base_url` 的 host 和 env 的 host 不同且没带 key → 不挪用 env key |
| 低：旧 `data.json` 顶层 `sidecarUrl` | `loadSettings` 兼容顶层字段 |
| nit：`merge_llm` 死分支 | 删掉 |
| nit：每键 `saveData` | 设置页 debounce 再写盘 |
| nit：对用户说「仓库根」 | 设置页 / README 改成人话 |
| nit：每次框选整本重建索引 | 同路径且未过期则 `refresh_if_stale`，不强制 `build_index` |

不做：Keychain、第二套 SDK、插件拉起 Python、插件里 apply。

## 复审补一刀

`merge_llm` 换主机返回 `None` 之后，`stream_chat` 不能再 `llm or resolve_llm()` 把 `.env` 钥匙拿回来。请求带了 `base_url` 就关掉 env 回退。`.obsidian` / `.env*` 比较不区分大小写。
