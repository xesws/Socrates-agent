# v0.13.1 — 审查：API 高于 minAppVersion

`Plugin.settings` 在 1.13 类型里才出现；设置页把 host 标成 `Plugin & { settings }` 会让每一个 `this.plugin.settings` 被判成新 API。改成自有 host 类型。`revealLeaf` 换成 `setActiveLeaf`（1.5.0 可用）。
