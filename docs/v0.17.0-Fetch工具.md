# v0.17.0 — 给 sidecar 注册 Fetch 工具

> 日期：2026-08-22。对应公开仓 `socrates-pen`。插件 semver 仍是 `0.14.0`，不发 Release。
> 本仓 `pen/` 与公开仓同步：第三把工具 `fetch`（GET 一个 http(s) URL，SSRF 闸）。

详细形状见公开仓 `docs/v0.17.0-Fetch工具.md`。
