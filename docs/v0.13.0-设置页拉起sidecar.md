# v0.13.0 — 设置页安装并拉起 sidecar

> 日期：2026-08-21。插件 0.13.0。公开仓 `socrates-pen`。

插件在 `~/.socrates-pen/venv` 里隔离安装本仓的 `pen`，并从设置页 / Obsidian 启动时 `spawn` `python -m pen`（只绑 loopback）。用户仍需本机已有 Python 3.11+。
